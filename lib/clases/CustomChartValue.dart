import 'package:pdf/pdf.dart';
import 'package:pdf/widgets.dart' as pw;

// Clase personalizada que extiende PointChartValue
class CustomChartValue extends pw.PointChartValue {
  CustomChartValue(double x, double y) : super(x, y);
}